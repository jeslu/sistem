<%= form_with(model: saledetail, url: [sale, saledetail]) do |form| %>
  <% if saledetail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(saledetail.errors.count, "error") %> prohibited this saledetail from being saved:</h2>

      <ul>
        <% saledetail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :sale_id, style: "display: block" %>
    <%= form.text_field :sale_id %>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    
        
    <%= form.collection_select(:product_id, Product.all, :id, :name_p, options = {}, {:class => "form-control"})%>
  </div>

  <div>
    <%= form.label :cantidad, style: "display: block" %>
    <%= form.number_field :cantidad %>
  </div>

  <div>
    <%= form.label :importe, style: "display: block" %>
    <%= form.text_field :importe %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
