<%= form_with(model: cuent_client) do |form| %>
  <% if cuent_client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cuent_client.errors.count, "error") %> prohibited this cuent_client from being saved:</h2>

      <ul>
        <% cuent_client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :client_id %>
     <%= form.collection_select(:client_id, Client.all, :id, :name, options = {}, {:class => "form-control"})%>
  </div>



  <div>
    <%= form.label :fecha %>
    <%= form.date_field :fecha, :class => "form-control" %>
  </div>

  <div>
    <%= form.label :active %>
    <%= form.check_box :active , checked: true%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
